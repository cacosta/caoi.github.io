---
---
callback([
{% for cat in site.categories | sort: title %}
  {
    "name": "{{site.cat[0] | replace:'-',' '}}",
    "value": "{{cat[0]}}"
  } {% unless forloop.last %},{% endunless%}
{% endfor %}
])
